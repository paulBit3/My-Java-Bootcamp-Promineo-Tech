package week1_projects;

import java.io.*;
import java.util.*;

public class AdditionMultiplication {
	/*
	 * Given the variables num1, num2, and num3, do the following:
	   Create an equation that adds num1 and num2 and then multiply 
	   that sum by num3
	   Assign that answer to a new variable named yourAnswer
	   Print the value of yourAnswer to the console
	 */

	public static void main(String[] args) {
		
		// User interaction
		Scanner in = new Scanner(System.in);
		int num1, num2, num3;
		
		//getting user input
		System.out.println("Enter number:");
		
		num1 = in.nextInt();
		num2 = in.nextInt();
		num3 = in.nextInt();
   	
		//computation
		int op = (num1 + num2) * num3;
		
		//yourAnswer variable
		int yourAnswer = op;
		
		System.out.println("You add " + num1 + " to " + num2 + " and x by " + num3 + ". Total is: " + + yourAnswer);

	}

}
